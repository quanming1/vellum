{
  "name": "my-monorepo",
  "version": "1.0.0",
  "description": "一个包含多个包的monorepo项目",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter test-app dev",
    "build": "pnpm -r build",
    "build:packages": "cd packages/utils-package && pnpm build && cd ../math-package && pnpm build && cd ../..",
    "build:test": "pnpm --filter test-app build",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
